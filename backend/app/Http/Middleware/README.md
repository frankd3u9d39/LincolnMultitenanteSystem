# Shared HTTP Middleware

- **Owner**: SHARED (All Groups)
- **Scope**: Request pipeline filters including Authentication, Tenant Context Resolution (`school_id`), Role/Permission gating, and CORS.
- **Rules**:
  - Middleware logic affects all routes.
  - Communicate with all group leads before introducing new middleware.
