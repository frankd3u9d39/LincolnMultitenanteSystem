# Shared Service Providers

- **Owner**: SHARED (All Groups)
- **Scope**: Core framework bootstrap providers (e.g. `AppServiceProvider`, multi-tenant service providers, event registration).
- **Rules**:
  - Shared architectural resource.
