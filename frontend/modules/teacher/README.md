# GROUP 2: Teacher Module

## Ownership
- **Assigned Group**: GROUP 2
- **Scope**: Dedicated feature logic, components, hooks, services, types, and utils for Teacher users.

## Directory Structure
```text
frontend/modules/teacher/
├── components/   # Teacher-specific UI widgets
├── hooks/        # Teacher-specific custom React hooks
├── services/     # Teacher-specific API integration services
├── types/        # Teacher domain TypeScript models/interfaces
├── utils/        # Teacher domain calculations & formatters
└── README.md
```

## Rules
1. Group GROUP 2 works primarily inside this directory.
2. Do NOT import directly into other module folders.
3. Consume shared UI elements from `frontend/components/`.
4. Route compositions are located under `frontend/app/(teacher)/`.
